# Conductor upstream capabilities

Observed upstream version: **0.3.0** on 2026-08-05.

The repository uses the current product, product-guidelines, tech-stack, workflow,
code-styleguide and track artefacts. It is prepared for adaptive host UX, smart
logical reversion and review-fix phases. The installer chooses Gemini CLI,
Antigravity or Claude Code according to the available host.

No documented upstream switch named `experimental` was found in the observed
version. Searchright therefore does not fabricate configuration keys. Host-level
plan mode, auto-update and future upstream capabilities may be enabled only after
the installed version is observed and this lock is refreshed.
