from searchright_contracts import BibliographicRecordIdentifiers

# Dynamic keys must remain usable with the declared value type.
identifiers: BibliographicRecordIdentifiers = {"other": {"custom": "value"}}
