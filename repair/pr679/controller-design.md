The final controller reads only from this staging branch, checks out the latest `main`, reconstructs the existing PR branch with `--force-with-lease`, copies the application and Track 36 pilot artefacts, removes the obsolete payload workflow, regenerates derived integrity files, runs the complete local gate set, pushes the exact validated commit, and polls the hosted checks. It never broadens the valid Conductor track range and never suppresses a failed gate.
