# Glama packet

The root `glama.json` declares GitHub maintainer `edithatogo`. After a public
repository and runnable release exist, authenticate through GitHub and submit the
repository. Glama builds/runs/introspects open-source servers, so a metadata-only
repository is not treated as accepted.
